import { StatusBoletoPipe } from './status-boleto.pipe';

describe('StatusBoletoPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusBoletoPipe();
    expect(pipe).toBeTruthy();
  });
});
